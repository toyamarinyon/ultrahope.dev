/goal packagemanagerをbunからpnpmに移行する。作業は [@builder](subagent://builder) に依頼してあなたはゴールまでのステップを整理して各ステップごとにsubagentのoutputが、あなたの期待値とのギャップが無くなるまでフィードバックを繰り返してください。

---

Goal: migrate this Next.js project's package manager from Bun to pnpm.

You are not alone in the codebase. Do not revert edits made by others; work with any existing changes.

Repository: /Users/toyamarinyon/.codex/worktrees/2c85/ultrahope.dev

Important repo instruction: AGENTS.md says this Next.js version may differ from training data and to read relevant docs in node_modules/next/dist/docs before writing code. For this task, read at least the App Router installation and next CLI docs:
- node_modules/next/dist/docs/01-app/01-getting-started/01-installation.md
- node_modules/next/dist/docs/01-app/03-api-reference/06-cli/next.md

Expected migration steps:
1. Update package.json packageManager from bun@1.3.13 to an appropriate pnpm version available in this environment. If pnpm is blocked by packageManager before the edit, inspect how to get the version after changing packageManager, or use corepack/pnpm conventions conservatively.
2. Replace Bun lockfile with pnpm lockfile: remove bun.lock and generate pnpm-lock.yaml using pnpm install.
3. Update project docs/references that instruct local commands from bun to pnpm, especially README.md and standards/styling.md. Preserve writing/blog content references unless they are project command docs; do not rewrite historical prose under writing/ unless clearly needed.
4. Verify with pnpm install, pnpm lint, and pnpm build if feasible. Report any command that could not run and why.

Please edit files directly in your workspace. Final output must include:
- files changed
- commands run and their results
- any remaining risks or follow-up needed

---

Review feedback: network-enabled `pnpm install` now reaches the registry and generated `pnpm-lock.yaml`, but it exits non-zero with `[ERR_PNPM_IGNORED_BUILDS] Ignored build scripts: sharp@0.34.5, unrs-resolver@1.12.2`. It also created `.pnpm-store/` and `pnpm-workspace.yaml` in the repo. Please close the remaining gap:

1. Inspect `pnpm-workspace.yaml` and any generated config. Decide whether it belongs in this single-package repo or was only generated because of local store config.
2. Fix the pnpm 11 ignored-builds failure in a repository-appropriate way, preferably committed config in `package.json`/pnpm config rather than relying on an interactive approval state. Allow only the packages actually needed (`sharp`, `unrs-resolver`) if that is the right pnpm mechanism.
3. Ensure no local cache/store directory like `.pnpm-store/` remains tracked or untracked unless intentionally documented.
4. Re-run `pnpm install`, then `pnpm lint`, then `pnpm build` if feasible.

Final output again: files changed, commands/results, and remaining risks.