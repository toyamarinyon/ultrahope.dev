/goal apps/web を rootにしたシンプルなNext.jsアプリケーションにしてください。
色々経緯があり、monorepoになっていますが、packagesにあったプロジェクトは全て別repositoryになり、apps/webだけになりました。無駄に階層が深くなっていて、色々不便なので、 apps/web をrootにしたシンプルな構成にしたいです。
実装は [@builder](subagent://builder) に依頼して、あなたはゴールまでの進み方を考えて、サブエージェントのアウトプットとあなたの期待とのギャップを埋めるようにフィードバックを繰り返してください。

実装が終わったら、`portless run next dev` で動作確認してください。
動作確認できたらbranchを作って、pull request作成までやってください。

---

実装担当として、/Users/toyamarinyon/.codex/worktrees/f01e/ultrahope.dev で apps/web をリポジトリ root にしたシンプルな Next.js アプリ構成へ変更してください。

要件:
- 現在 monorepo だが apps/web だけが残っているため、apps/web 配下の Next.js アプリを root に移動する。
- root の package.json は workspace/turbo ではなく単体 Next.js アプリとして整理する。scripts は apps/web/package.json 相当の dev/build/start/lint と、既存 root の format/format:check を必要なら残す。
- turbo.json は不要なら削除。
- apps/web の app, public, writing, lib, proxy.ts, next.config.ts, postcss.config.mjs, eslint.config.mjs, tsconfig.json など必要ファイルを root へ移す。
- 既存の README/AGENTS/CLAUDE/standards/biome/vercel 設定など root 固有のファイルは保持する。
- import/path が apps/web 前提で壊れないか確認し、必要なら修正する。
- bun.lock を新しい package.json に合わせて更新できるなら更新する。ネットワークが必要な場合は無理に実行せず、その旨を報告する。
- Next.js 16.2.2 です。AGENTS.mdにある通り、もし node_modules/next/dist/docs/ が利用可能なら関連ドキュメントを読んでからコードを書いてください。見当たらない場合はその事実を報告してください。
- 他者の変更を revert しないでください。

完了時に、変更したファイル一覧、削除したファイル一覧、実行した検証コマンドと結果、未解決事項を報告してください。PR作成やbranch作成、dev server検証は親エージェント側で行います。

---

調整したのでやってみてください。